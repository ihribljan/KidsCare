<section id="login">
    <div class="login-container">
        <span>
           {$poruka}
           {if $as == 4}
                <a href="?pg=prijava">
                    <button type="button" class="btnPrijava">PRIJAVI SE</button>
                </a>
           {/if}
        </span>
    </div>
</section>